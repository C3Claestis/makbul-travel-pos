import 'package:flutter/material.dart';

class PaketumrahPage extends StatelessWidget {
  const PaketumrahPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}